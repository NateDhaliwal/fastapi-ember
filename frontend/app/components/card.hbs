<div class="card">
  <div class="card-header">
    <h3>{{@title}}</h3>
  </div>
  <div class="card-body">
    <span class="checkbox {{if @done 'checked' 'unchecked'}}"></span>
    <p>{{@description}}</p>
  </div>
</div>